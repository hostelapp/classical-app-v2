# Front-end environment variables
# Set when serving the app from a sub-path such as GitHub Pages
# VITE_BASE_PATH="/classical-app-v2/"

# Point to a remote JSON catalog. When omitted, the built-in catalog in public/catalog.json is used.
# VITE_CATALOG_URL="https://example.com/catalog.json"

# Enable the admin studio in production builds. Leave unset (or set to "false") when publishing publicly.
# VITE_ENABLE_ADMIN="false"
